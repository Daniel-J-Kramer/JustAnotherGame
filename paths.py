import pygame
import time
import random
from strings import *
from getText import getText
#End import list
#Begin variables list
#End variables list
#Begin game code

def pathChange(path):
    return path

def choiceAAA(pathA, pathB, pathC, pathD) -> tuple: #Answer Yes to Play Game
    pathA = pathChange(choiceAAE)
    pathB = pathChange(choiceAAF)
    pathC = pathChange(choiceAAG)
    pathD = pathChange(choiceAAH)
    return pathA, pathB, pathC, pathD

def choiceAAB(pathA, pathB, pathC, pathD): #Answer No to Play Game
    pygame.QUIT

def choiceAAC(pathA, pathB, pathC, pathD) -> tuple: #Answer Change Text Speed to Play Game
    return pathA, pathB, pathC, pathD

def choiceAAD(pathA, pathB, pathC, pathD) -> tuple: #Answer Change Color Scheme to Play Game
    return pathA, pathB, pathC, pathD

def choiceAAE(pathA, pathB, pathC, pathD) -> tuple:
    return pathA, pathB, pathC, pathD

def choiceAAF(pathA, pathB, pathC, pathD) -> tuple:
    return pathA, pathB, pathC, pathD

def choiceAAG(pathA, pathB, pathC, pathD) -> tuple:   
    return pathA, pathB, pathC, pathD

def choiceAAH(pathA, pathB, pathC, pathD) -> tuple:
    return pathA, pathB, pathC, pathD